# Document Handling - Computer Vision Project
[![Python](https://img.shields.io/badge/Python-3.13%2B-blue)](https://www.python.org/)
[![OpenCV](https://img.shields.io/badge/OpenCV-4.12.0-green)](https://opencv.org/)
[![NumPy](https://img.shields.io/badge/NumPy-2.2.6-blue)](https://numpy.org/)
____
Проект для обработки и анализа документов с использованием компьютерного зрения. Включает функции для обнаружения документа на изображении, перспективной трансформации и анализа текстового содержимого.
____
## 🎯 Основные задачи
- 🔍 Обнаружение документа: Автоматическое нахождение угловых точек документа на изображении
- 📐 Перспективная трансформация: Выравнивание документа с исправлением искажений перспективы
- 📊 Анализ текста: Подсчет количества строк и символов в документе
____
## :white_check_mark: Условия задачи
- Выбрать в сетевой папке изображение с документом (указать в таблице фамилию напротив изображения, у каждого свое изображение).
- С помошью `OpenCV` найти угловые точки документа.
- Зная угловые точки сделать перспективную трансформацию для того чтобы выравнять документ (можно использовать функцию `warpperspective`).
- На выравненом изображении документа посчитать количество строк и символов (реализовать свой алгоритм, без использования готовых `OCR`).
  
Например: https://learnopencv.com/automatic-document-scanner-using-opencv/
____
## 🏗️ Структура проекта

Document handling/
- 📁 core/
  - init.py
  - document_transformation.py
  - text_analyzer.py
- 📁 utils/
  - init.py
  - file_handler.py
- 📁 models/
  - init.py
  - document.py
- 📁 shared/
  - init.py
  - constants.py
  - load_library.py
- 📁 data/
  - IMG_3024.jpeg
- 📁 results/
  - img_doc_23.27.05.png
- main.py
- README.md
____
## 🚀 Быстрый старт
### Установка зависимостей 📦
```
pip install opencv-python numpy
```

### Базовое использование 💻
```
from core.document_transformation import DocumentTransformation
from core.text_analyzer import TextAnalyzer
from utils.file_handler import FileHandler

# Получаем путь к входному изображению
img_path = FileHandler.select_file_path()

# Обнаружение и трансформация документа
transform = DocumentTransformation()
result = transform.process_document(img_path)
# Сохранение документа
FileHandler.save_image(image=result.get_transformed_img())

# Анализ текста
analyzer = TextAnalyzer()
result = analyzer.process_document(result)
# Сохранение документа
FileHandler.save_image(image=result.get_selected_text_img(), copy=True)
FileHandler.save_info_text(result)
```

### 📁 Выбор файлов
Проект предоставляет удобный интерфейс для выбора изображений через системный диалог реализованный через стандартный интерфейс Python `tkinter`:

```
from utils.file_handler.py import FileHandler

# Открытие диалога выбора файла
image_path = FileHandler.select_file_path()
```
Описание функции из класса `FileHandler` в [utils.file_handler.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/utils/__init__.py)
```
def select_file_path():
  """
  Метод для выбора изображения в файловой системе
    
  Входные значения (Inputs):
    - Нет явных параметров
    - Пользовательский ввод через диалоговое окно выбора файла
    
  Выходные значения (Outputs):
    
    УСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Возвращает: str (полный путь к выбранному файлу изображения)
        Пример: "C:/Users/username/Pictures/image.jpg"
    
    НЕУСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Завершает программу через sys.exit("Файл не выбран")
      - Не возвращает управление вызывающему коду
  """
```
Особенности:
- 📁 Графический интерфейс для выбора файлов
- 🖼️ Поддержка популярных форматов изображений (JPEG, PNG, BMP)
- ⚡ Автоматическая проверка выбора файла
- 🛡️ Обработка случаев, когда файл не выбран

Поддерживаемые форматы можно настроить в [shared.constants.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/shared/constants.py):
```
FORMAT_IMAGE_FILE = "*.jpg *.jpeg *.png *.bmp"
```  

### 🔄 Обнаружение и трансформация документа
Модуль `DocumentTransformation` обеспечивает полный цикл обработки документа - от загрузки изображения до выравнивания перспективы с использованием компьютерного зрения.
```
from core.document_transformation import DocumentTransformation

# Создание экземпляра трансформера
transform = DocumentTransformation()

# Основной метод для преобразования документа
result = transform.process_document(img_path)
```
Детали реализации из [core.file_handler.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/core/__init__.py)
```
def process_document(self, image_path):
  """
  Обрабатывает документ: загружает изображение, определяет границы документа,
  применяет перспективное преобразование для выравнивания.
  
  Входные значения (Inputs):
    image_path (str): Путь к файлу изображения для обработки
        
  Выходные значения (Outputs):
    
    УСПЕШНОЕ ВЫПОЛНЕНИЕ:
      Возвращает: Document object - обработанный документ с выровненным изображением
    
    НЕУСПЕШНОЕ ВЫПОЛНЕНИЕ:
      Возвращает: tuple (bool, str) - (False, "сообщение об ошибке") в случаях:
        - Не удалось загрузить изображение
        - Не найдено достаточно углов для преобразования
            
    ИСКЛЮЧЕНИЯ:
      sys.exit: Завершает программу при любых других ошибках с сообщением об ошибке
  """
```
Особенности:
- 🧩 Автоматическое определение ориентации документа
- 📏 Коррекция искажений перспективы
- 🎯 Точное выделение границ даже при сложном фоне
- ⚡ Оптимизированная обработка для быстрого выполнения
### 📊 Анализ текста
Модуль `TextAnalyzer` предоставляет комплексный анализ текстового содержимого документа с подсчетом строк и символов.
```
from core.text_analyzer import TextAnalyzer

# Создание экземпляра анализатора
analyzer = TextAnalyzer()

# Основной метод для анализа текста в документе
analyzer.process_document(result)
```
Детали реализации из [core.text_analyzer.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/core/text_analyzer.py)
```
def process_document(self, document):
  """
  Анализирует текст на документе: выделяет строки и слова/символы,
  визуализирует результаты обнаружения.
    
  Входные значения (Inputs):
    document (Document): Объект документа с выровненным изображением
        
  Выходные значения (Outputs):
    
  УСПЕШНОЕ ВЫПОЛНЕНИЕ:
    Возвращает: list of tuples - список кортежей с координатами строк
    Пример: [(y_start1, y_end1), (y_start2, y_end2), ...]
    
  ИСКЛЮЧЕНИЯ:
    sys.exit: Завершает программу при любых ошибках с сообщением об ошибке
  """
```
Особенности:
- 📄 Выделение текстовых областей - идентификация основных блоков текста
- ➖ Сегментация строк - точное определение границ каждой строки текста
- 🔤 Разделение на символы - детальная сегментация текстовых элементов
- 🎨 Визуализация результатов - цветное выделение обнаруженных элементов
### 💾 Сохранение результатов
Проект предоставляет гибкую систему сохранения результатов обработки документов:
```
from utils.file_handler import FileHandler

# Сохранение обработанного изображения
success = FileHandler.save_image(processed_image)
```
Описание функции `save_image` и `save_info_text` из [utils.file_handler.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/utils/__init__.py)
```
def save_image(image, copy=False):
  """
  Автоматически сохраняет изображение в папку с временной меткой

  Входные значения (Inputs):
    image (numpy.ndarray): Массив изображения для сохранения
    copy  (bool): Переменая для создания копии (используется для сохранения изображения с выделенными строками и символами)
        
  Выходные значения (Outputs):
    
    УСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Возвращает: bool (True при успешном сохранении)
      - Создает структуру папок: results/ГГГГ-ММ-ДД/
      - Генерирует имя файла: img_doc_ЧЧ.ММ.СС.png
      - Выводит путь к сохраненному файлу в консоль
    
    НЕУСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Возвращает: bool (False при ошибке сохранения)
      - Может вызвать исключение при проблемах с файловой системой
  """
```
```
def save_info_text(document):
  """
  Метод для сохранения информации о количестве строк и символах

  Входные значения (Inputs):
    document (Object Document): Класс документа

    Выходные значения (Outputs):
    
    УСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Возвращает: bool (True при успешном сохранении)
      - Создает структуру папок: results/ГГГГ-ММ-ДД/
      - Генерирует имя файла: info_doc_ЧЧ.ММ.СС.txt
    
    НЕУСПЕШНОЕ ВЫПОЛНЕНИЕ:
      - Возвращает: bool (False при ошибке сохранения)
      - Может вызвать исключение при проблемах с файловой системой

  """
```
Особенности реализации:
- 📅 Автоматическая организация по датам - создает папки в формате results/ГГГГ-ММ-ДД/
- ⏰ Уникальные имена файлов - использует временные метки для избежания конфликтов
- 🗂️ Автоматическое создание директорий - не требует предварительной настройки путей
- 📝 Логирование путей - выводит полный путь к сохраненному файлу
- 🖼️ Формат PNG - сохраняет без потерь качества
____
## 📋 Функциональность
1. Обнаружение документа
   * Предобработка изображения (билинейная интерполяция, фильтрация)
   * Обнаружение контуров и угловых точек
   * Автоматическое определение границ документа
2. Перспективная трансформация
   * Коррекция перспективных искажений
   * Выравнивание документа
   * Адаптивное масштабирование
3. Анализ текста
   * Подсчет строк текста
   * Подсчет символов
   * Визуализация результатов
____
## 🛠️ Технологический стек
- OpenCV 4.12.0 - обработка изображений и компьютерное зрение
- NumPy 2.2.6 - численные вычисления и работа с массивами
____
## 📊 Пример работы
Исходное изображение → Обнаруженные углы → Выровненный документ → Анализ текста"

|<dir>|<dir>|
|---|---|
| 1. Загрузка исходного изображения | 2. Обнаруженние углов |
| <img src="https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/data/IMG_3024.jpeg" alt="Исходное изображение"> | <img src="https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/results/Image%20corners.png" alt="Изображение с выделенными угловыми точками"> |
| 3. Выравнивание документа | 4. Анализ текста |
| <img src="https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/results/02.11.2025/img_doc_17.55.40.png" alt="Выровненный документ"> | <img src="https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/results/02.11.2025/img_doc_17.55.40copy.png" alt="Документ с выделиными строками и символами"> |

____
## 🔧 Настройка
Параметры обработки можно настроить в [shared.constants.py](https://github.com/Not-broken-today/CV-Completed-tasks/blob/main/Document%20handling/shared/constants.py):

###  Максимальный размер изображения для обработки (необходим для оптимизации при работе с изображениями в высоком разрешении)
```
MAX_DIMENSION = 1000
```
### Поддерживаемые форматы:
```
FORMAT_IMAGE_FILE = "*.jpg *.jpeg *.png *.bmp"
```
### Настройка директории для сохранения результатов путей
```
OUTPUT_PATH = "results/"
```
____
## 📈 Результаты
После обработки в папке results/ сохраняются:
- Выровненное изображение документа
- Визуализация обнаруженных контуров (опционально)
- Статистика анализа текста

